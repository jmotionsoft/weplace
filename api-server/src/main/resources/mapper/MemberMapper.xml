<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="towntalk.mapper.MemberDao">
    <select id="getMember" parameterType="member" resultType="member">

    </select>

    <select id="getMemberFromPassword" parameterType="member" resultType="member">

    </select>

    <select id="countMember" parameterType="member" resultType="int">

    </select>

    <insert id="createMember" parameterType="member" useGeneratedKeys="true" keyProperty="user_no">

    </insert>

    <update id="editMember" parameterType="member">

    </update>

    <delete id="removeMember" parameterType="member">

    </delete>

</mapper>