<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="towntalk.mapper.TempPasswordDao">
    <select id="getTempPassword" parameterType="tempPassword" resultType="tempPassword">

    </select>

    <insert id="createTempPassword" parameterType="tempPassword">

    </insert>

    <delete id="deleteTempPassword" parameterType="tempPassword">

    </delete>
</mapper>